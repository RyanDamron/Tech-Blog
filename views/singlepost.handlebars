<div>
    <div data-post-id="{{postId}}">
        <h4><a href="/posts/{{post.id}}">{{post.name}}</a></h4>
        <h6>{{post.text}}<br>Posted by: {{user.username}} on: {{post.date_created}}</h6>
        {{#if comments.length}}
        {{#each comments as |comment|}}
        <p>{{comment.text}}{{username}}</p>
        {{/each}}
        {{/if}}
    </div>
    {{!-- <div>
        <form class="form new-comment-form">
            <div class="form-group">
                <label for="comment-text">Add a Comment: </label>
                <input class="form-input" type="text" id="comment-input" name="comment-text" />
            </div>
            <div class="form-group">
                <button class="btn btn-primary" id="comment-submit-btn" type="submit" data-id="{{{id}}}">Add
                    Comment</button>
            </div>
        </form>
    </div> --}}
</div>

<script src="/js/comment.js"></script>